import {connect} from "react-redux";
import PersonViewProfessionImpl from "./PersonViewProfessionImpl";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(PersonViewProfessionImpl);